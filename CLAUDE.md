# CLAUDE.md

이 문서는 `elldia/todo` 프로젝트를 진행하면서 Claude Code와 나눈 대화 기록과, 각 요청을 Claude가 어떻게 이해하고 어떤 순서로 처리했는지를 정리한 기록입니다. 이후 프로젝트를 이어받거나 다시 살펴볼 때 맥락을 빠르게 파악할 수 있도록 남깁니다.

## 대화 기록 정리

### 1. 새 프로젝트 경로 확인 요청

**질문 (사용자)**
> git@github.com:elldia/todo.git 이 주소에 지금부터 새로운 프로젝트를 만들거야. 경로를 확인하기만 해.

**Claude의 이해와 처리**
- 사용자가 "경로를 확인하기만 해"라고 했으므로, 아직 실제로 무언가를 생성/변경하라는 지시가 아니라 **현재 작업 위치를 점검해달라는 요청**으로 해석함.
- `pwd`, `git rev-parse --show-toplevel`, `git remote -v`를 실행해 현재 디렉터리(`.../kosa-vibecoding-2026-4th/src/exercise/elldia`)가 클래스룸 공유 저장소(`weable-kosa/kosa-vibecoding-2026-4th`) 내부에 있다는 것을 확인.
- 사용자가 지정한 `elldia/todo.git`은 이 클래스룸 저장소와는 별개의 원격 저장소이므로, 새 프로젝트를 이 서브폴더 안에 만들 것인지 완전히 별도 경로에 만들 것인지 사용자에게 되물음(판단이 필요한 갈림길이라 임의로 정하지 않음).

**답변 (Claude)**
- 현재 위치와 클래스룸 저장소 origin 정보를 안내하고, 별도 경로로 만들지 서브폴더로 만들지 확인 요청.

---

### 2. 별도 경로에 초기화 결정

**질문 (사용자)**
> ubun/work/todo 별도로 초기화해서 진행할게

**Claude의 이해와 처리**
- "별도로 초기화"라는 표현을 클래스룸 저장소와 완전히 독립된 새 git 저장소를 `/home/ubun/work/todo` 경로에 만드는 것으로 해석.
- 기존에 해당 경로가 있는지 먼저 확인(`ls`)한 뒤, 없는 것을 확인하고 나서 디렉터리 생성.
- `git init` → `git remote add origin git@github.com:elldia/todo.git` → 기본 브랜치명이 `master`로 생성된 것을 확인하고, GitHub 및 이 워크스페이스 관례(`main`)에 맞춰 `git branch -m main`으로 변경.
- 커밋은 아직 없는 빈 저장소 상태임을 최종 확인.

**답변 (Claude)**
- 저장소 초기화 완료, origin 연결 완료, 브랜치 `main`으로 정리했다고 안내하고 다음 작업(무엇을 만들지) 대기.

---

### 3. Todo 앱 구현 + README 작성 요청

**질문 (사용자)**
> HTML, CSs, JS만으로 구성되는 todo앱을 구현해줘. README.md파일엔 todo앱을 사용하는데 이용한 기술과 사용자 입장에서 todo 앱을 사용하는 간편 매뉴얼을 작성해줘

**Claude의 이해와 처리**
- "HTML, CSS, JS만으로"라는 제약을 빌드 도구나 프레임워크, 외부 라이브러리 없이 **순수 정적 파일 3종(`index.html`, `style.css`, `script.js`)** 으로 구현해야 하는 요건으로 해석.
- 별도 백엔드 요청이 없었으므로, 데이터 영속성은 브라우저의 `localStorage`를 사용하는 방식으로 설계 (새로고침해도 목록이 유지되도록).
- 기본적인 Todo 앱에 기대되는 핵심 기능을 스스로 판단하여 포함:
  - 할 일 추가 (폼 제출)
  - 완료 체크 (체크박스 토글)
  - 텍스트 더블클릭으로 수정
  - 개별 삭제
  - 필터링 (전체 / 진행 중 / 완료)
  - 완료 항목 일괄 삭제
  - 남은 할 일 개수 표시
- 구현 후 `node --check script.js`로 문법 오류가 없는지 점검(실행 로직 자체를 브라우저로 직접 클릭 테스트하지는 않았고, 문법 검증만 수행함 — 이 부분은 사용자가 브라우저에서 직접 확인 필요).
- README.md는 "이용한 기술"과 "사용자 입장의 간편 매뉴얼" 두 파트로 나눠 작성:
  - 기술: HTML5 / CSS3 / Vanilla JavaScript / Web Storage API(localStorage), 빌드 과정 없음을 명시.
  - 매뉴얼: 추가/완료 처리/수정/삭제/필터링/일괄 삭제/개수 확인 순서로 사용자 시나리오 기준 설명.

**답변 (Claude)**
- 생성한 파일 목록과 핵심 기능을 요약해서 안내하고, 브라우저에서 직접 열어 확인해볼 것을 권장. 커밋/푸시는 명시적 요청이 있을 때만 진행하겠다고 안내(레포 정책 준수).

---

### 4. CLAUDE.md / WORKFLOW.md 작성 요청

**질문 (사용자)**
> CLAUDE.md 파일을 생성하고 클러드와 내가 대화한 질문/답변 기록을 정리하고 클러드가 내 답변을 어떻게 이해하고 풀어나갔는지를 자세하고 일목요연하게 정리해서 만들어줘. 또, WORKFLOW.md 파일을 만들어서 todo 앱을 제작하기 위한 단계를 처음부터 다시 제작해야한다면 어떤 순서대로 해야하는지 개발자 매뉴얼 식으로 작성해줘.

**Claude의 이해와 처리**
- 두 개의 별도 문서를 요구하는 것으로 해석:
  1. **CLAUDE.md** — 지금까지의 대화(질문/답변) 히스토리와, 각 질문을 Claude가 어떤 논리로 해석하고 실행했는지에 대한 회고적 기록(현재 이 문서).
  2. **WORKFLOW.md** — 실제 코드 이력이 아니라, "처음부터 다시 만든다면"이라는 가정 하에 재현 가능한 순서로 정리한 개발자용 절차 매뉴얼.
- 이번 대화 내용 전체(1~4번 항목)를 순서대로 되짚어 정리하는 방식으로 CLAUDE.md를 구성.

---

## 참고

- 이 저장소는 `git@github.com:elldia/todo.git`을 origin으로 하는 독립 저장소이며, `kosa-vibecoding-2026-4th` 클래스룸 저장소와는 무관합니다.
- git commit/push는 사용자가 해당 턴에서 명시적으로 요청할 때만 수행합니다.
